fn main() {
    println!(letra());
}

fn letra() -> struct{
    struct holi {
        val: String,
        per: String,
        yan: i64,
        tr: struct
    }
    struct test {
        name: &str,
        age: i64
    }
    return holi{
        val: "jaja".to_string(),
        per: "holis".to_string(),
        yan: 666,
        tr: test {
            name: "juan",
            age: 26
        } };
}