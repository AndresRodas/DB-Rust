fn main() {
    struct persona{
                name: &str,
                age: i64
            }
    let mut vtest : Vec<persona> = vec![persona{ name: "andy", age: 26 }, persona{ name: "miriam", age: 23 }];
    println!("{}",test(vtest, 1));
}
fn test(tabla: Vec<persona>, id: i64)-> Vec<persona>{
    return tabla[id];
}
